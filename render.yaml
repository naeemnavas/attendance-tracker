services:
  - type: web
    name: sh-attendance-tracker
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m playwright install chromium --with-deps
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
